# This was generated by tool. Edits will be overwritten.

name: bff/ci
on:
  workflow_dispatch:
  push:
    paths:
    - .github/workflows/bff-**
    - bff/**
    - Directory.Packages.props
  pull_request:
    paths:
    - .github/workflows/bff-**
    - bff/**
    - Directory.Packages.props
env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
jobs:
  verify-formatting:
    name: Verify formatting
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        shell: bash
        working-directory: bff
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: |-
          8.0.x
          9.0.103
    - name: Restore
      run: dotnet restore bff.slnf
    - name: Verify Formatting
      run: dotnet format bff.slnf --verify-no-changes --no-restore
  build:
    name: Build and test (unit)
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    permissions:
      actions: read
      checks: write
      contents: read
      packages: write
    defaults:
      run:
        shell: bash
        working-directory: bff
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: |-
          8.0.x
          9.0.103
    - name: Restore
      run: dotnet restore bff.slnf
    - name: Build
      run: dotnet build bff.slnf --no-restore -c Release
    - name: Dotnet devcerts
      run: dotnet dev-certs https --trust
    - name: Test - test/Bff.Tests
      run: dotnet test test/Bff.Tests -c Release --no-build --logger "console;verbosity=normal" --logger "trx;LogFileName=test/Bff.Tests-tests.trx" --collect:"XPlat Code Coverage"
    - id: test-report-test-Bff-Tests
      name: Test report - test/Bff.Tests
      if: github.event_name == 'push' && (success() || failure())
      uses: dorny/test-reporter@31a54ee7ebcacc03a09ea97a7e5465a47b84aea5
      with:
        name: Test Report - test/Bff.Tests
        path: '**/test/Bff.Tests-tests.trx'
        reporter: dotnet-trx
        fail-on-error: true
        fail-on-empty: true
    - name: Publish test report link
      run: echo "[Test Results - test/Bff.Tests](${{ steps.test-report-test-Bff-Tests.outputs.url_html }})" >> $GITHUB_STEP_SUMMARY
    - name: Test - test/Bff.Blazor.Client.UnitTests
      run: dotnet test test/Bff.Blazor.Client.UnitTests -c Release --no-build --logger "console;verbosity=normal" --logger "trx;LogFileName=test/Bff.Blazor.Client.UnitTests-tests.trx" --collect:"XPlat Code Coverage"
    - id: test-report-test-Bff-Blazor-Client-UnitTests
      name: Test report - test/Bff.Blazor.Client.UnitTests
      if: github.event_name == 'push' && (success() || failure())
      uses: dorny/test-reporter@31a54ee7ebcacc03a09ea97a7e5465a47b84aea5
      with:
        name: Test Report - test/Bff.Blazor.Client.UnitTests
        path: '**/test/Bff.Blazor.Client.UnitTests-tests.trx'
        reporter: dotnet-trx
        fail-on-error: true
        fail-on-empty: true
    - name: Publish test report link
      run: echo "[Test Results - test/Bff.Blazor.Client.UnitTests](${{ steps.test-report-test-Bff-Blazor-Client-UnitTests.outputs.url_html }})" >> $GITHUB_STEP_SUMMARY
    - name: Test - test/Bff.Blazor.UnitTests
      run: dotnet test test/Bff.Blazor.UnitTests -c Release --no-build --logger "console;verbosity=normal" --logger "trx;LogFileName=test/Bff.Blazor.UnitTests-tests.trx" --collect:"XPlat Code Coverage"
    - id: test-report-test-Bff-Blazor-UnitTests
      name: Test report - test/Bff.Blazor.UnitTests
      if: github.event_name == 'push' && (success() || failure())
      uses: dorny/test-reporter@31a54ee7ebcacc03a09ea97a7e5465a47b84aea5
      with:
        name: Test Report - test/Bff.Blazor.UnitTests
        path: '**/test/Bff.Blazor.UnitTests-tests.trx'
        reporter: dotnet-trx
        fail-on-error: true
        fail-on-empty: true
    - name: Publish test report link
      run: echo "[Test Results - test/Bff.Blazor.UnitTests](${{ steps.test-report-test-Bff-Blazor-UnitTests.outputs.url_html }})" >> $GITHUB_STEP_SUMMARY
    - name: Test - test/Bff.EntityFramework.Tests
      run: dotnet test test/Bff.EntityFramework.Tests -c Release --no-build --logger "console;verbosity=normal" --logger "trx;LogFileName=test/Bff.EntityFramework.Tests-tests.trx" --collect:"XPlat Code Coverage"
    - id: test-report-test-Bff-EntityFramework-Tests
      name: Test report - test/Bff.EntityFramework.Tests
      if: github.event_name == 'push' && (success() || failure())
      uses: dorny/test-reporter@31a54ee7ebcacc03a09ea97a7e5465a47b84aea5
      with:
        name: Test Report - test/Bff.EntityFramework.Tests
        path: '**/test/Bff.EntityFramework.Tests-tests.trx'
        reporter: dotnet-trx
        fail-on-error: true
        fail-on-empty: true
    - name: Publish test report link
      run: echo "[Test Results - test/Bff.EntityFramework.Tests](${{ steps.test-report-test-Bff-EntityFramework-Tests.outputs.url_html }})" >> $GITHUB_STEP_SUMMARY
  playwright:
    name: Playwright tests
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    permissions:
      actions: read
      checks: write
      contents: read
    defaults:
      run:
        shell: bash
        working-directory: bff
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: |-
          8.0.x
          9.0.103
    - name: Restore
      run: dotnet restore bff.slnf
    - name: Build
      run: dotnet build bff.slnf --no-restore -c Release
    - name: Install Playwright
      run: pwsh test/Hosts.Tests/bin/Release/net9.0/playwright.ps1 install --with-deps
    - name: Dotnet devcerts
      run: dotnet dev-certs https --trust
    - name: Test - test/Hosts.Tests
      run: dotnet test test/Hosts.Tests -c Release --no-build --logger "console;verbosity=normal" --logger "trx;LogFileName=test/Hosts.Tests-tests.trx" --collect:"XPlat Code Coverage"
    - id: test-report-test-Hosts-Tests
      name: Test report - test/Hosts.Tests
      if: github.event_name == 'push' && (success() || failure())
      uses: dorny/test-reporter@31a54ee7ebcacc03a09ea97a7e5465a47b84aea5
      with:
        name: Test Report - test/Hosts.Tests
        path: '**/test/Hosts.Tests-tests.trx'
        reporter: dotnet-trx
        fail-on-error: true
        fail-on-empty: true
    - name: Publish test report link
      run: echo "[Test Results - test/Hosts.Tests](${{ steps.test-report-test-Hosts-Tests.outputs.url_html }})" >> $GITHUB_STEP_SUMMARY
    - name: Upload playwright traces
      if: success() || failure()
      uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882
      with:
        name: playwright-traces
        path: bff/test/**/playwright-traces/*.zip
        overwrite: true
        retention-days: 15
  codeql:
    name: CodeQL analyze
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: bff
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Initialize CodeQL
      uses: github/codeql-action/init@9e8d0789d4a0fa9ceb6b1738f7e269594bdd67f0
      with:
        languages: csharp
        build-mode: manual
        db-location: ~/.codeql/databases
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: |-
          8.0.x
          9.0.103
    - name: Restore
      run: dotnet restore bff.slnf
    - name: Build
      run: dotnet build bff.slnf --no-restore -c Release
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@9e8d0789d4a0fa9ceb6b1738f7e269594bdd67f0
      with:
        category: /language:csharp
  pack:
    name: Pack, sign and push
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    needs:
    - verify-formatting
    - build
    - playwright
    - codeql
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      packages: write
    defaults:
      run:
        shell: bash
        working-directory: bff
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: |-
          8.0.x
          9.0.103
    - name: Tool restore
      run: dotnet tool restore
    - name: Pack bff.slnf
      run: dotnet pack -c Release bff.slnf -o artifacts
    - name: Sign packages
      if: github.event == 'push'
      run: |-
        for file in artifacts/*.nupkg; do
           dotnet NuGetKeyVaultSignTool sign "$file" --file-digest sha256 --timestamp-rfc3161 http://timestamp.digicert.com --azure-key-vault-url https://duendecodesigninghsm.vault.azure.net/ --azure-key-vault-client-id 18e3de68-2556-4345-8076-a46fad79e474 --azure-key-vault-tenant-id ed3089f0-5401-4758-90eb-066124e2d907 --azure-key-vault-client-secret ${{ secrets.SignClientSecret }} --azure-key-vault-certificate NuGetPackageSigning
        done
    - name: Push packages to GitHub
      if: github.ref == 'refs/heads/main'
      run: dotnet nuget push artifacts/*.nupkg --source https://nuget.pkg.github.com/DuendeSoftware/index.json --api-key ${{ secrets.GITHUB_TOKEN }} --skip-duplicate
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Upload Artifacts
      if: github.ref == 'refs/heads/main'
      uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882
      with:
        name: artifacts
        path: bff/artifacts/*.nupkg
        overwrite: true
        retention-days: 15
